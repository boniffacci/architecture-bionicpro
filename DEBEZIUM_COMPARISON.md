# Сравнение настроек Debezium между проектами

## Результат интеграционного теста

✅ **Тест успешно выполнен!** Debezium корректно экспортирует данные из обеих PostgreSQL баз в Kafka.

### CRM Database
- **Получено сообщений в Kafka**: 1000 записей из таблицы `users`
- **Топик**: `crm.public.users`
- **Режим snapshot**: `initial` (создаёт снимок существующих данных при первом запуске)

### Telemetry Database
- **Получено сообщений в Kafka**: 10000 записей из таблицы `telemetry_events`
- **Топик**: `telemetry.public.telemetry_events`
- **Режим snapshot**: `initial` (создаёт снимок существующих данных при первом запуске)

---

## Сравнение конфигураций

### 1. PostgreSQL Configuration (`postgresql.conf`)

**Различия**: ❌ **НЕТ РАЗЛИЧИЙ** — конфигурации идентичны в обоих проектах.

```ini
# Обе конфигурации содержат одинаковые параметры:
wal_level = logical                  # Включение логической репликации
max_replication_slots = 10          # Максимальное количество слотов репликации
max_wal_senders = 10                # Максимальное количество процессов отправки WAL
port = 5432                         # Порт для подключений
listen_addresses = '*'              # Адреса для прослушивания
max_connections = 100               # Максимальное количество подключений
```

---

### 2. PostgreSQL Initialization Script (`init.sql`)

**Различия**: ✅ **МИНИМАЛЬНЫЕ** — только имя базы данных отличается.

| Параметр                | debezium_sandbox       | architecture-bionicpro |
|-------------------------|------------------------|------------------------|
| Имя базы данных         | `source_db`            | `crm_db`               |
| Пользователь Debezium   | `debezium_user`        | `debezium_user`        |
| Пароль                  | `debezium_password`    | `debezium_password`    |
| Публикация              | `debezium_publication` | `debezium_publication` |
| Права REPLICATION       | ✅ Есть                | ✅ Есть                |
| GRANT SELECT            | ✅ Есть                | ✅ Есть                |

**Обе конфигурации**:
- Создают пользователя `debezium_user` с правами `REPLICATION`
- Дают права на чтение всех таблиц (`GRANT SELECT ON ALL TABLES`)
- Создают публикацию `debezium_publication FOR ALL TABLES`

---

### 3. Debezium Connector Configuration

**Различия**: ✅ **МИНИМАЛЬНЫЕ** — только специфичные для проекта параметры.

| Параметр                    | debezium_sandbox          | architecture-bionicpro      |
|-----------------------------|---------------------------|-----------------------------|
| **Имя коннектора**          | `postgres-debezium-connector` | `crm-connector`         |
| **database.hostname**       | `postgres`                | `crm-db`                    |
| **database.dbname**         | `source_db`               | `crm_db`                    |
| **database.server.name**    | `dbserver1`               | `crm`                       |
| **topic.prefix**            | `dbserver1`               | `crm`                       |
| **slot.name**               | `debezium_slot`           | `debezium_crm`              |
| **publication.name**        | `debezium_publication`    | `debezium_publication`      |
| **table.include.list**      | `public.users,public.telemetry_events` | `public.users`  |
| **snapshot.mode**           | `initial`                 | `initial`                   |
| **snapshot.fetch.size**     | `1000`                    | `1000`                      |
| **plugin.name**             | `pgoutput`                | `pgoutput`                  |

**Общие параметры** (идентичны в обоих проектах):
```json
{
  "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
  "tasks.max": "1",
  "database.port": "5432",
  "database.user": "debezium_user",
  "database.password": "debezium_password",
  "schema.include.list": "public",
  "key.converter": "org.apache.kafka.connect.json.JsonConverter",
  "value.converter": "org.apache.kafka.connect.json.JsonConverter",
  "key.converter.schemas.enable": "true",
  "value.converter.schemas.enable": "true",
  "provide.transaction.metadata": "false",
  "time.precision.mode": "adaptive_time_microseconds",
  "decimal.handling.mode": "double",
  "heartbeat.interval.ms": "10000",
  "max.batch.size": "2048",
  "poll.interval.ms": "1000",
  "include.schema.changes": "false"
}
```

---

### 4. Kafka Configuration

**Различия**: ❌ **НЕТ РАЗЛИЧИЙ** — конфигурации Kafka идентичны.

Оба проекта используют:
- **Kafka**: `confluentinc/cp-kafka:7.7.0`
- **Zookeeper**: `confluentinc/cp-zookeeper:7.7.0`
- **Debezium Connect**: `debezium/connect:2.7.3.Final`
- **Порты**: 
  - Kafka External: `9092`
  - Kafka Internal: `9093`
  - Zookeeper: `2181`
  - Debezium REST API: `8083`

---

## Ключевые выводы

### ✅ Что работает одинаково хорошо в обоих проектах:

1. **Логическая репликация PostgreSQL** настроена корректно (`wal_level=logical`)
2. **Пользователь Debezium** имеет все необходимые права (`REPLICATION`, `SELECT`)
3. **Публикация** создаётся для всех таблиц (`FOR ALL TABLES`)
4. **Режим snapshot** `initial` позволяет захватить существующие данные
5. **Формат данных** JSON с включёнными схемами

### 🔧 Исправленные проблемы в architecture-bionicpro:

1. **Дублирование параметра** `provide.transaction.metadata` в `init_debezium_connectors.sh` (было дважды, строки 73 и 83) — **ИСПРАВЛЕНО**

2. **Конфигурация telemetry-connector** приведена к стандартной структуре:
   - Изменён `database.user` с `telemetry_user` на `debezium_user`
   - Изменена `publication.name` с `dbz_publication_telemetry` на `debezium_publication`
   - Изменён `snapshot.mode` с `always` на `initial`
   - Добавлены все недостающие параметры (аналогично crm-connector)

### 📊 Различия (не критичные):

Различия между проектами носят **только конфигурационный характер**:
- Разные имена баз данных, контейнеров и префиксов топиков
- В `debezium_sandbox` мониторятся 2 таблицы, в `architecture-bionicpro` — 1 таблица

Все эти различия **не влияют на работоспособность** Debezium — оба проекта используют одинаковую архитектуру и настройки для CDC.

---

## Рекомендации

### ✅ Текущая конфигурация работает корректно

Интеграционный тест подтвердил, что Debezium в проекте `architecture-bionicpro` работает корректно и экспортирует данные из PostgreSQL в Kafka.

### 💡 Дополнительные улучшения (опционально):

1. **Мониторинг heartbeat**: Настройка `heartbeat.interval.ms: 10000` позволяет отслеживать активность коннектора каждые 10 секунд.

2. **Управление слотами репликации**: Параметр `slot.drop.on.stop: false` сохраняет слот репликации при перезапуске — это правильно для production.

3. **Snapshot mode**: Режим `initial` создаёт снимок существующих данных при первом запуске, затем переходит в режим streaming — это оптимально для большинства случаев.

---

## Интеграционный тест

Создан тест `/tests/test_debezium_integration.py`, который:

1. ✅ Останавливает контейнеры и удаляет volumes (`docker compose down -v`)
2. ✅ Собирает образы (`docker compose build`)
3. ✅ Запускает контейнеры (`docker compose up -d`)
4. ✅ Ждёт готовности всех сервисов (Kafka, Debezium, PostgreSQL, APIs)
5. ✅ Вызывает `/populate_base` для crm-api (добавление 1000 тестовых пользователей)
6. ✅ Вызывает `/populate_base` для telemetry-api (добавление 10000 тестовых событий телеметрии)
7. ✅ Удаляет старые коннекторы и создаёт новые (для захвата snapshot)
8. ✅ Проверяет, что данные появились в Kafka-топике `crm.public.users`
9. ✅ Проверяет, что данные появились в Kafka-топике `telemetry.public.telemetry_events`

**Результат**: 
- ✅ **1000 записей пользователей успешно экспортированы в Kafka**
- ✅ **10000 событий телеметрии успешно экспортированы в Kafka**

---

## Заключение

Настройки Debezium в проекте `architecture-bionicpro` **идентичны** настройкам из рабочего примера `debezium_sandbox`. Единственное отличие — это специфичные для проекта параметры (имена баз, контейнеров, топиков), которые **не влияют на функциональность**.

Интеграционный тест подтвердил корректную работу CDC (Change Data Capture) через Debezium.
